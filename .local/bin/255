#!/bin/bash

# Set the starting number
num=1

# Loop through all files ending in .png
for file in *.png
do
  # Rename the file by replacing the current number with the desired number
  newname=$(echo "$file" | sed "s/[0-9]*\.png/$num.png/")
  mv "$file" "$newname"

  # Increment the number
